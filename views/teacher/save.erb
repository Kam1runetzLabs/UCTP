<!DOCTYPE html>
<%#include "teacher.h" %>
<% tfetch(QVariantMap, teacher); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>Editing Teacher</h1>

<%== formTag(urla("save", teacher["id"]), Tf::Post) %>
  <p>
    <label>ID<br /><input type="text" name="teacher[id]" value="<%= teacher["id"] %>" readonly="readonly" /></label>
  </p>
  <p>
    <label>TeacherName<br /><input type="text" name="teacher[teacherName]" value="<%= teacher["teacherName"] %>" /></label>
  </p>
  <p>
    <input type="submit" value="Save" />
  </p>
</form>

<%== linkTo("Show", urla("show", teacher["id"])) %> |
<%== linkTo("Back", urla("index")) %>
</body>
</html>
