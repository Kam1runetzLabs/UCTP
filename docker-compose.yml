version: '3'
services:
  postgres:
    image: postgres
    ports:
      - 5432:5432
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./db-data:/var/lib/postgresql/data
      - ./db:/docker-entrypoint-initdb.d
